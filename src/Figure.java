public class
Figure {
    public double calcArea() {
        return 0;
    }

    public static boolean compare(Figure f1, Figure f2) {
               if (f1.calcArea() == f2.calcArea()) return true;
        else return false;
    }
}
